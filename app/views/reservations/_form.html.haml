.reservation-form
  %h4 Reserve this class for...
  = form_for [@schedule, @reservation] do |f|
    = render 'shared/error_messages', object: f.object
    .field
      = f.collection_select :child_id, @children, :id, :first_name, {}, { class: "width-100 form-control" }
    .pull-right
      = f.submit "Reserve", class: "gp-button"
