= simple_form_for(@static_page_content) do |f|
  = f.error_notification

  .form-inputs
    = f.input :key, input_html: {placeholder: 'key'}
    = f.input :value, as: :bootsy, input_html: {rows: 10}

  .form-actions
    = f.button :submit, class: 'btn btn-success'
