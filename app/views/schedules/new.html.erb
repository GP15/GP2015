<% provide(:title, 'Add schedule') %>

<h1>Add Schedule</h1>

<%= form_for [@partner, @schedule] do |f| %>

  <%= render 'form', f: f %>

  <div class="actions">
    <%= link_to "Cancel", admin_partner_path(@partner) %>
    <%= f.submit "Create" %>
  </div>
<% end %>
