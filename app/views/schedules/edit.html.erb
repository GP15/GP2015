<% provide(:title, 'Edit schedule') %>

<section class="padding-one">
  <div class="container">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">

        <h1 class="title-large text-center margin-four-bottom">Edit Schedule</h1>

        <%= form_for [@partner, @schedule] do |f| %>
          <%= render 'form', f: f %>

          <div class="margin-five pull-right">
            <%= link_to "Cancel", admin_partner_path(@partner),
                class: "btn-small-black-border-light btn btn-small button" %>
            <%= f.submit "Update", class: "highlight-button-dark btn btn-small button" %>
          </div>
        <% end %>

      </div>
    </div>
  </div>
</section>
