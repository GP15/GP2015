= simple_form_for(@zipcode) do |f|
  = f.error_notification

  .form-inputs
    %label
    = f.input :pincode, input_html: {placeholder: 'Pincode'}

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
