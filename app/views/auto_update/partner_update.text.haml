Hi 

= @schedule.klass.partner.company
= @schedule.activity.name
= @schedule.start_at.to_date
= #{@schedule.start_at.hour}:#{@schedule.start_at.min}

------------------


No of participants #{@schedule.children.count}

%h3 Childrens
- @schedule.children.each do |child|
  = #{child.first_name} #{child.last_name} (#{Time.now.year - child.birth_year.to_i})

